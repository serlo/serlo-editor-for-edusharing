import fetch from 'node-fetch'

describe('Editor tests', () => {
    const port = parseInt(process.env.PORT, 10) || 3000
    const url = `http://localhost:${port}`

    test.todo('Open editor with LTI', () => {
        
    })

    test.todo('Open editor in view mode when postContentApiUrl is not present ', () => {
        
    })

    test.todo('Button "Close and Save"', () => {
        
    })

    test.todo('Button "name version"', () => {
        
    })

    test.todo('Content is saved after 5 secs', () => {
        
    })

    test.todo('Content is saved when closed with a tab', () => {
        
    })
})